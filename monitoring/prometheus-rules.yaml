# Prometheus Alert Rules
# NOTE: All alerting is handled by AWS CloudWatch Alarms (see pulumi/src/cloudwatch-alarms.ts)
# This file is kept for potential recording rules in the future
# Prometheus is used only for metrics collection, not alerting

groups:
  - name: node-fleet-metrics
    interval: 30s
    rules: []
