# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
venv/
env/
ENV/

# Pulumi
.pulumi/
Pulumi.*.yaml
!Pulumi.yaml

# AWS
*.pem
*.key

# Secrets
.env
.env.local
secrets/
*.secret

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# GitHub Copilot (personal AI assistant instructions)
.github/copilot-instructions.md

# Logs
*.log
logs/

# Terraform (if used)
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl

# K3s
kubeconfig
*.kubeconfig

# Test outputs
k6-results/
test-reports/
htmlcov/
test-results/
tests/lambda/test_output.txt
tests/test_results.txt
tests/lambda/.coverage
tests/lambda/.pytest_cache/

# Pulumi temp/generated/log files
pulumi/pulumi_error.log
pulumi/pulumi_full.log
pulumi/*.b64
pulumi/dist/

# Kubeconfig files (security risk)
full_kubeconfig.yaml
temp_kubeconfig.yaml
kubeconfig.yaml
*.kubeconfig

# Private keys (already covered by *.pem but be explicit)
node-fleet-key.pem
pulumi/key.pem

# Lambda deployment packages
lambda/function.zip
lambda/package/
lambda/*.dist-info/
lambda/*.dist-info
lambda/bin/
lambda/venv/
lambda/build_check/
lambda/package_build/
lambda/tests/
# Installed Python deps in lambda/ (pip -t installs)
lambda/requests/
lambda/urllib3/
lambda/certifi/
lambda/charset_normalizer/
lambda/idna/
lambda/cryptography/
lambda/paramiko/
lambda/bcrypt/
lambda/cffi/
lambda/nacl/
lambda/pynacl-*.dist-info/
lambda/yaml/
lambda/_yaml/
lambda/six.py
lambda/six-*.dist-info
lambda/kubernetes/
lambda/google/
lambda/oauthlib/
lambda/requests_oauthlib/
lambda/pyasn1/
lambda/pyasn1_modules/
lambda/dateutil/
lambda/websocket/
lambda/pycparser/
lambda/*.pyd
lambda/*.so
lambda/httmock.py
lambda/pylab.py

# Node modules (if any)
node_modules/

# Temporary files
tmp/
temp/
*.tmp
kubeconfig.yaml
.coverage
.venv/
